Common Factors - Descending Order

#The program must accept two positive integers X and Y as the input.
#The program must print the common factors of X and Y in the descending order.

#code
x,y=map(int,input().split())
for i in range(x+1,0,-1):
    if x%i==0 and y%i==0:
        print(i,end=" ")
